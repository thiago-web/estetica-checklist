// MOSTRAR MOTO OU CARRO
  $('input[name="opt_luzes"]').change(function () {
    if ($('input[name="opt_luzes"]:checked').val() === "Bom") {
    $('.obs_luzes').hide();
    } else {
    $('.obs_luzes').show();
    }
    });
// MOSTRAR MOTO OU CARRO

// LUZES DE FREIO
$('input[name="opt_luzes"]').change(function () {
    if ($('input[name="opt_luzes"]:checked').val() === "Bom") {
    $('.obs_luzes').hide();
    } else {
    $('.obs_luzes').show();
    }
    });
// LUZES DE FREIO

// LANTERNAS
  $('input[name="opt_lanternas"]').change(function () {
    if ($('input[name="opt_lanternas"]:checked').val() === "Bom") {
    $('.obs_lanternas').hide();
    } else {
    $('.obs_lanternas').show();
    }
    });
//  LANTERNAS

// SETAS
  $('input[name="opt_setas"]').change(function () {
    if ($('input[name="opt_setas"]:checked').val() === "Bom") {
    $('.obs_setas').hide();
    } else {
    $('.obs_setas').show();
    }
    });
// SETAS

// LIMPADORES
  $('input[name="opt_limpadores"]').change(function () {
    if ($('input[name="opt_limpadores"]:checked').val() === "Bom") {
    $('.obs_limpadores').hide();
    } else {
    $('.obs_limpadores').show();
    }
    });
// LIMPADORES

// PARACHOQUE
  $('input[name="opt_parachoque"]').change(function () {
    if ($('input[name="opt_parachoque"]:checked').val() === "Bom") {
    $('.obs_parachoque').hide();
    } else {
    $('.obs_parachoque').show();
    }
    });
// PARACHOQUE

// VIDROS TRASEIROS
  $('input[name="opt_vidros_traseiro"]').change(function () {
    if ($('input[name="opt_vidros_traseiro"]:checked').val() === "Bom") {
    $('.obs_vidros_traseiro').hide();
    } else {
    $('.obs_vidros_traseiro').show();
    }
    });
// VIDROS TRASEIROS

// PLACA
$('input[name="opt_placa"]').change(function () {
    if ($('input[name="opt_placa"]:checked').val() === "Bom") {
    $('.obs_placa').hide();
    } else {
    $('.obs_placa').show();
    }
    });
// PLACA